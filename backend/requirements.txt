# =============================================
# DermAI Backend - Python Bağımlılıkları
# =============================================
# Kurulum: pip install -r requirements.txt
# =============================================

# Web framework ve ASGI sunucusu
fastapi==0.111.0
uvicorn[standard]==0.30.0

# Multipart form verisi (görüntü yükleme için zorunlu)
python-multipart==0.0.9

# Ortam değişkenleri (.env dosyası okuma)
python-dotenv==1.0.1

# Derin öğrenme - AI model çalıştırmak için
torch>=2.0.0
torchvision>=0.15.0

# Görüntü işleme
Pillow>=10.0.0

# Model özet ve parametre sayısı (ai-model/model.py tarafından kullanılır)
torchinfo>=1.8.0
