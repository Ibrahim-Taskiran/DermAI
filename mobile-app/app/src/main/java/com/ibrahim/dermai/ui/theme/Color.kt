package com.ibrahim.dermai.ui.theme

import androidx.compose.ui.graphics.Color

// ── DermAI Marka Renkleri ──────────────────────────────────────
// Sağlık / dermatoloji temasına uygun teal-cyan-yeşil paleti

// Primary – Ana marka rengi (koyu teal)
val DermPrimary = Color(0xFF0D9488)          // Teal 600
val DermPrimaryLight = Color(0xFF14B8A6)     // Teal 500
val DermPrimaryDark = Color(0xFF0F766E)      // Teal 700
val DermOnPrimary = Color(0xFFFFFFFF)

// Secondary – Vurgulama rengi (cyan)
val DermSecondary = Color(0xFF06B6D4)        // Cyan 500
val DermSecondaryLight = Color(0xFF22D3EE)   // Cyan 400
val DermSecondaryDark = Color(0xFF0891B2)    // Cyan 600
val DermOnSecondary = Color(0xFFFFFFFF)

// Tertiary – Üçüncül renk (indigo aksan)
val DermTertiary = Color(0xFF6366F1)         // Indigo 500
val DermOnTertiary = Color(0xFFFFFFFF)

// Surface / Background – Açık modda
val DermBackground = Color(0xFFF0FDFA)       // Teal 50
val DermSurface = Color(0xFFFFFFFF)
val DermSurfaceVariant = Color(0xFFF1F5F9)   // Slate 100
val DermOnBackground = Color(0xFF0F172A)     // Slate 900
val DermOnSurface = Color(0xFF1E293B)        // Slate 800
val DermOnSurfaceVariant = Color(0xFF475569) // Slate 600

// Konteyner Renkleri
val DermPrimaryContainer = Color(0xFFCCFBF1)     // Teal 100
val DermOnPrimaryContainer = Color(0xFF134E4A)   // Teal 900
val DermSecondaryContainer = Color(0xFFCFFAFE)   // Cyan 100
val DermOnSecondaryContainer = Color(0xFF164E63) // Cyan 900
val DermTertiaryContainer = Color(0xFFE0E7FF)    // Indigo 100
val DermOnTertiaryContainer = Color(0xFF312E81)  // Indigo 900

// Hata / Uyarı Renkleri
val DermError = Color(0xFFDC2626)            // Red 600
val DermErrorContainer = Color(0xFFFEE2E2)   // Red 100
val DermOnError = Color(0xFFFFFFFF)
val DermOnErrorContainer = Color(0xFF991B1B) // Red 800

// Outline
val DermOutline = Color(0xFFCBD5E1)          // Slate 300
val DermOutlineVariant = Color(0xFFE2E8F0)   // Slate 200

// ── Dark Mode Renkleri ─────────────────────────────────────────

val DermPrimaryDarkMode = Color(0xFF2DD4BF)        // Teal 400
val DermOnPrimaryDarkMode = Color(0xFF042F2E)      // Teal 950

val DermSecondaryDarkMode = Color(0xFF22D3EE)      // Cyan 400
val DermOnSecondaryDarkMode = Color(0xFF083344)     // Cyan 950

val DermTertiaryDarkMode = Color(0xFF818CF8)       // Indigo 400
val DermOnTertiaryDarkMode = Color(0xFF1E1B4B)     // Indigo 950

val DermBackgroundDark = Color(0xFF0F172A)         // Slate 900
val DermSurfaceDark = Color(0xFF1E293B)            // Slate 800
val DermSurfaceVariantDark = Color(0xFF334155)     // Slate 700
val DermOnBackgroundDark = Color(0xFFF1F5F9)       // Slate 100
val DermOnSurfaceDark = Color(0xFFE2E8F0)          // Slate 200
val DermOnSurfaceVariantDark = Color(0xFF94A3B8)   // Slate 400

val DermPrimaryContainerDark = Color(0xFF115E59)   // Teal 800
val DermOnPrimaryContainerDark = Color(0xFF99F6E4) // Teal 200
val DermSecondaryContainerDark = Color(0xFF155E75) // Cyan 800
val DermOnSecondaryContainerDark = Color(0xFFA5F3FC) // Cyan 200
val DermTertiaryContainerDark = Color(0xFF3730A3)  // Indigo 800
val DermOnTertiaryContainerDark = Color(0xFFC7D2FE) // Indigo 200

val DermErrorDark = Color(0xFFFCA5A5)              // Red 300
val DermErrorContainerDark = Color(0xFF7F1D1D)     // Red 900
val DermOnErrorDark = Color(0xFF450A0A)            // Red 950
val DermOnErrorContainerDark = Color(0xFFFECACA)   // Red 200

val DermOutlineDark = Color(0xFF475569)            // Slate 600
val DermOutlineVariantDark = Color(0xFF334155)     // Slate 700

// ── Gradient Renkleri ──────────────────────────────────────────
val GradientStart = Color(0xFF0D9488)   // Teal 600
val GradientMiddle = Color(0xFF06B6D4)  // Cyan 500
val GradientEnd = Color(0xFF6366F1)     // Indigo 500

val GradientSplashStart = Color(0xFF042F2E)  // Teal 950
val GradientSplashMiddle = Color(0xFF0F766E) // Teal 700
val GradientSplashEnd = Color(0xFF0D9488)    // Teal 600

// Başarı rengi
val DermSuccess = Color(0xFF16A34A)     // Green 600
val DermSuccessContainer = Color(0xFFDCFCE7) // Green 100
val DermWarning = Color(0xFFF59E0B)     // Amber 500
val DermWarningContainer = Color(0xFFFEF3C7) // Amber 100